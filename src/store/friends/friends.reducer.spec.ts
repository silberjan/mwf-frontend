import { friendsInitialState, FriendsState } from './friends.reducer'

describe('Transit Lines Reducer', () => {
  let initialState: FriendsState

  beforeEach(() => {
    initialState = friendsInitialState
  })
})
